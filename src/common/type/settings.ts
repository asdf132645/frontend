export interface WbcActiveSettingType {
    runningCount: boolean;
    customClass: boolean;
    normalRange: boolean;
    classOrder: boolean;
}

export interface HotkeyActiveSettingType {
    wbc: boolean;
    bf: boolean;
}