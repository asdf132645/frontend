export interface WbcActiveSettingType {
    runningCount: boolean;
    customClass: boolean;
    normalRange: boolean;
    classOrder: boolean;
    hotkey: boolean;
}

export interface AddOnsSettingType {
    downloadUpload: boolean;
    etc: boolean;
}

export interface HotkeyActiveSettingType {
    wbc: boolean;
    bf: boolean;
}

export interface LisActiveSettingType {
    hotkeyFilePath: boolean;
    code: boolean;
}

export interface CbcActiveSettingType {
    filePath: boolean;
    code: boolean;
}